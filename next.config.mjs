/** @type {import('next').NextConfig} */
const nextConfig = {
  async rewrites() {
    // return [
    //   {
    //     source: "/:path*",
    //     destination: "/pemodal/:path*",
    //   },
    // ];
  },
};

export default nextConfig;
